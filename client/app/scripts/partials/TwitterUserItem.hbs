<div class="col-md-4">
    <div class="thumbnail">
      <img src="{{profile_image_url}}" >
      <div class="caption">
        <h3>{{name}}</h3>
        <h4>{{location}}</h4>
        <p><a href="#twitter-user/{{screen_name}}" class="btn btn-primary" role="button">View Profile</a> <a href="#" class="btn btn-default" role="button">Button</a></p>
      </div>
    </div>
  </div>